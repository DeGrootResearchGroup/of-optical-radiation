#!/bin/sh
cd "${0%/*}" || exit 1

. "$WM_PROJECT_DIR/bin/tools/CleanFunctions"

cleanCase
rm -rf postProcessing constant/polyMesh constant/triSurface
# Defensive: Allrun-DOM swaps system/controlDict for the DOM step and
# restores it on exit; clean up if a crash leaked the backup.
rm -f system/controlDict.flow.bak

# ----------------------------------------------------------------------------
