#!/bin/sh
cd "${0%/*}" || exit 1

. "$(dirname "$0")/../lib/AllcleanCommon.sh"

# splitMeshRegions writes per-region polyMesh trees; cleanCase only
# touches the top-level constant/polyMesh.
rm -rf constant/mediumA/polyMesh constant/mediumB/polyMesh

# ----------------------------------------------------------------------------
